package jp.pokemonms.controller.authentication;

import org.junit.Test;
import org.slim3.tester.ControllerTestCase;

/**
 * AuthenticationCallbackControllerTest
 * @author Sinmetal
 *
 */
public class CallbackControllerTest extends ControllerTestCase {

    /**
     * TwitterOAuthのテスト方法が分からないため保留
     * @throws Exception
     */
    @Test
    public void run() throws Exception {
//        tester.start("/authentication/callback");
//        CallbackController controller = tester.getController();
//        assertThat(controller, is(notNullValue()));
//        assertThat(tester.isRedirect(), is(false));
//        assertThat(tester.getDestinationPath(), is(nullValue()));
    }
}
