package jp.pokemonms.controller.storage;

import org.junit.Test;
import org.slim3.tester.ControllerTestCase;

public class EditControllerTest extends ControllerTestCase {

    @Test
    public void run() throws Exception {
//        tester.start("/storage/edit");
//        EditController controller = tester.getController();
//        assertThat(controller, is(notNullValue()));
//        assertThat(tester.isRedirect(), is(false));
//        assertThat(tester.getDestinationPath(), is("/storage/Edit.jsp"));
    }
}
